apiVersion: batch/v1
kind: CronJob
metadata:
  name: cypress-automation-cron-edge
  namespace: cypress-automation
spec:
  schedule: "0 6 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: cypress-automation-edge
            image: adriandez/cypress-automation:edge
            imagePullPolicy: "Always"
            env:
              - name: INSTALL_BROWSER
                value: edge
              - name: TEST_BROWSER
                value: edge
              - name: TEST_ENV
                value: testing
              - name: ENABLE_VIDEO
                value: 'true'
            envFrom:
              - configMapRef:
                  name: cypress-cron-env
            securityContext:
              privileged: true
          imagePullSecrets:
          - name: regcred
          restartPolicy: Never
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
